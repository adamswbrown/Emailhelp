# macOS Email Accounting CLI & Native App - Dependencies
#
# Python 3.7+ required
#
# Standard library modules used (no pip install needed):
# - sqlite3 (built-in)
# - email (built-in)
# - argparse (built-in)
# - pathlib (built-in)
# - re (built-in)
# - datetime (built-in)
#
# Optional dependency for native macOS app:
pywebview>=4.0  # For native window application (main_app.py)
#
# Install with: pip3 install pywebview
#
# Note: Core CLI functionality (main.py) has NO external dependencies.
# PyWebView is only required for the native GUI app (main_app.py).
